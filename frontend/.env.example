# Copy to .env and fill in the shared API access token privately.
# The shared AWS URL in shared-api.js is the default; override it here if needed.
VITE_API_BASE_URL=
VITE_API_TOKEN=
# VITE_ values are included in frontend builds. Never use AWS or Supabase secrets here.
